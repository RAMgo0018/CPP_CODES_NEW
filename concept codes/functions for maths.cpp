#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    double num,root;
    cout<<"enter the number";
    cin>>num;
    root=sqrt(num);
    cout<<"the square of"<<num<<"is"<<root;
    return 0;

}